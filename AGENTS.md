<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Repo facts (verified 2026-09-19)

- Stack: Next.js 16.3.5 + React 19 + TypeScript 5 + Tailwind CSS 4 + ESLint 9, App Router with `src/` dir (`src/app/page.tsx`, `src/app/layout.tsx`, `src/app/globals.css`).
- Package manager: npm (`package-lock.json` present). Git repo initialized 2026-09-19 (commit awal `50463b2`); `.env*` ignored (`.env.example` force-added sebagai template).
- Build aman tanpa env: `src/lib/auth.ts` lazy (`getAuth()`), verified `npm run build` lolos tanpa `.env.local`.
- Scripts: `npm run dev` (next dev), `npm run build` (next build, Turbopack, verified OK), `npm start` (next start), `npm run lint` (eslint, verified OK).
- Config: `next.config.ts`, `tsconfig.json` (path alias `@/*` -> `src/*`), `postcss.config.mjs` (@tailwindcss/postcss), `eslint.config.mjs` (eslint-config-next).
- Planned: Neon Postgres (SQL gratis) + deploy Vercel Hobby. Next step: install `@neondatabase/serverless` + Drizzle/Prisma, tambah tabel accounts/transactions/rates.
- DB layer (Neon-ready, tanpa DB tetap jalan): `src/db/schema.ts` (accounts, categories, transactions, rates), `src/db/client.ts` (null-safe jika `DATABASE_URL` kosong), `drizzle.config.ts` (out `./drizzle`), migrasi awal `drizzle/0000_gorgeous_warstar.sql` (verified via `drizzle-kit generate`).
- App (localStorage-first, verified dev/build/lint + smoke test `next start`): `src/lib/finance.ts` (tipe, konversi IDR, agregasi hari/minggu/bulan/tahun, seed), `src/lib/useLedger.ts` (state + persist), UI M3 di `src/components/` + `src/app/page.tsx` (dashboard, akun, statistik recharts, FAB catat).
- API: `GET /api/health` (ping Neon, `db:true` hanya jika reachable), `GET /api/rates` (USDTIDR via Binance→CoinGecko, emas via gold-api.com, cache 5-10 mnt, fallback konstanta; verified live 2026-09-19).
- API Neon-backed (verified CRUD live 2026-09-19, saldo konsisten): `GET|POST /api/accounts`, `PATCH|DELETE /api/accounts/[id]`, `GET|POST /api/transactions` (update saldo via SQL delta + snapshot amountIdr), `DELETE /api/transactions/[id]` (revert saldo), `POST /api/ledger/replace` (tulis ulang buku kas: adopsi lokal→Neon, contoh, hapus semua; amountIdr opsional). `src/lib/ledger-api.ts` = helper fetch client. `useLedger` Neon-first: hidrasi lokal instan → load server → auto-replace jika server kosong & lokal berisi (dengan `normalizeIds` karena kolom Neon bertipe uuid + retry 3x untuk transient service_overload); tiap mutasi mirror background via thunk + 1 retry, gagal → mode lokal. ID pakai `crypto.randomUUID` (sama dengan PK Neon).
- Status 2026-09-19 malam: badge "Neon DB" live, server kosong karena user menghapus data contoh via UI (add/delete verified jalan dua arah). Klik "Muat contoh" di footer untuk isi ulang contoh ke Neon.
- Auth multi-user (Better Auth email+password, verified 2026-09-19): tabel `"user"`, `session`, `account`, `verification` + `accounts.user_id` (nullable untuk klaim baris yatim); migrasi `drizzle/0001_talented_justin_hammer.sql`. `src/lib/auth.ts` (server), `src/lib/auth-client.ts` (`useSession` tanpa provider), `src/app/api/auth/[...all]/route.ts`, `src/lib/require-user.ts` (`authUser()` + klaim yatim). SEMUA API ledger 401 jika belum login + scope `userId` (tx via join). Isolasi A/B verified via curl ( &
signup, 401 anon, password salah). UI: `AuthScreen` (Masuk/Daftar M3) + gate di `page.tsx` + tombol Keluar. Env: `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` (wajib di Vercel dengan URL production). Akun BCA asli user = yatim (user_id null), otomatis diklaim saat user login pertama.
- Fix bocor antar-user (2026-09-19): cache localStorage di-namespace per userId (`etw-accounts-v1:<uid>`); user baru mulai kosong (seed contoh hanya untuk pemakaian pertama); saat online cache lama pra-login dibuang, saat offline dipakai apa adanya. `useLedger(userId)` dari `session.user.id`. Verified: user C baru → `[]`, tidak mewarisi data A/B.
- Fix seed bocor ke user baru (2026-09-19, live): auto-seed contoh DIHAPUS total — tiap device baru dulu men-seed lalu adopsi ke user baru (temuan: teman user daftar via URL production langsung dapat data contoh). Sekarang user baru selalu `[]`. `seedData`/`reseed` tersisa untuk dev manual.
- Rencana tersimpan (belum dibangun): bot Telegram struk-OCR. Fase 1: webhook `/api/telegram/webhook` + pairing kode 6-digit + foto→Gemini Flash vision (free tier)→konfirmasi tombol (income/expense→akun→kategori)→simpan. Fase 2: koreksi nominal via chat, /ringkas mingguan, split item multi-kategori. Butuh: token @BotFather + Gemini API key (user pasang sendiri di env). Tabel baru saat dibangun: `bot_pending`, `user.telegram_chat_id` + tabel kode pairing.
- Bot Telegram Fase 1 DIBANGUN (2026-09-19, belum live perlu token): `POST /api/telegram/webhook` (verifikasi `x-telegram-bot-api-secret-token`, selalu 200; state di `bot_pending`, callback ≤64B: `k:`, `a:<uuid>`, `c:<idx>`, `ok`/`no`; foto→`parseReceipt` Gemini→tombol kind→akun (live Neon)→kategori→konfirmasi→simpan via `createTransaction`, konversi IDR→native USDT/emas via kurs), `GET|POST /api/pairing` (kode 6-digit 10 mnt, `crypto.randomInt`), UI `TelegramSection`. Tabel: `pairing_codes`, `bot_pending`, `user.telegram_chat_id` (migrasi `drizzle/0002_slimy_polaris.sql`, applied manual karena push butuh TTY untuk unique-baru). Shared: `src/lib/ledger-service.ts` (`createTransaction`), `src/lib/rates-server.ts` (`getRates`), `src/lib/telegram.ts`, `src/lib/gemini.ts`. Tanpa env → 503 + UI "belum dikonfigurasi" (verified). Env baru: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_BOT_USERNAME` (opsional), `GEMINI_API_KEY`, `GEMINI_MODEL` (default gemini-2.5-flash). Setup user: @BotFather token + AI Studio key + setWebhook ke `<PROD>/api/telegram/webhook`.
- Deps runtime: `@neondatabase/serverless`, `drizzle-orm`, `recharts`; dev: `drizzle-kit`. Env: `.env.example` (`DATABASE_URL` untuk Neon).
- Production (Vercel live): badge provider DB hanya render saat `NODE_ENV=development`; footer tanpa sebut provider. Tab Masuk/Daftar: aktif = `bg-primary` bold + ✓, nonaktif transparan.
- Rekonsiliasi saldo (verified 2026-09-20): tombol "Sesuaikan" per akun → input saldo aktual → pratinjau selisih live → `adjustBalance` catat selisih sbg tx kategori "Penyesuaian" (income/expense ikut arah). Server: `POST /api/accounts/[id]/adjust` atomik (set absolut + insert tx id client, tanpa double-hitung); naik/turun/nol verified via curl.
- Koreksi vs catat-via-saldo (2026-09-20, gantikan Penyesuaian): "Koreksi" = betulkan angka diam-diam (tanpa tx, `updateAccount` biasa). Dialog "Catat" punya toggle mode [💸 Nominal | ⚖️ Saldo aktual]: mode saldo → input saldo aktual → selisih vs tercatat jadi transaksi normal dengan kategori bebas (arah ikut tanda). Endpoint adjust + kategori Penyesuaian DIHAPUS (pakai `POST /api/transactions` biasa).
- Kategori dinamis (2026-09-20): `GET|POST /api/categories` (global, auth, POST idempoten per nama); UI via `useCategories` (fallback statis offline) + tambah custom (nama + preset ikon) di dialog Catat; warna custom dari hash nama (`colorForCategory`); bot baca tabel langsung. Baris yang ditambah manual di Neon otomatis muncul (verified: "Fee Burn").
- Gaji rutin (2026-09-20): tabel `recurring_rules` (user, akun, kind, nominal, kategori, tgl 1-31, startMonth, active) + `recurring_runs` (unique rule+period, migrasi `drizzle/0003_previous_black_cat.sql`). Efektif: jepit akhir bulan + maju ke Jumat bila Sab/Min (`effectiveMonthlyDate`, verified: 25 Jan 2026 Min→23, 25 Apr Sab→24, 30 Feb→27). `processDueRules` idempoten (klaim runs dulu, lepas klaim bila gagal) + backfill ≤6 bln. API: `GET|POST /api/recurring`, `PATCH|DELETE /api/recurring/[id]`, `POST /api/recurring/run` (auth, auto-run saat buka app + `reload()`), `GET /api/cron/recurring` (CRON_SECRET via header/query). Cron Vercel Hobby harian: `vercel.json` `0 22 * * *` (05:00 WIB). UI `RecurringSection` (tambah/ubah/toggle/hapus, pratinjau 3 bln + catatan maju, "Proses sekarang"). Verified: run→1, run→0, cron 403/200, tx backdate + saldo benar.
- Lokalisasi ID/EN (2026-09-20): `src/i18n/dict.ts` (kunci flat, kelengkapan EN dicek compiler) + `src/i18n/lang.tsx` (`LangProvider`, `useLang().t()` + interpolasi `{var}`, `LangToggle` segmented di header sebelah Keluar, persist `etw-lang-v1`). Semua komponen chrome-UI pakai `t()`; tanggal pakai locale aktif; nama kategori & data tidak diterjemahkan.
